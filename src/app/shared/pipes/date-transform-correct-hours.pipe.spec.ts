import { DateTransformCorrectHoursPipe } from './date-transform-correct-hours.pipe';

describe('DateTransformCorrectHoursPipe', () => {
  it('create an instance', () => {
    const pipe = new DateTransformCorrectHoursPipe();
    expect(pipe).toBeTruthy();
  });
});
